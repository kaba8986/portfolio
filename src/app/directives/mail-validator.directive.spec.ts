import { MailValidatorDirective } from './mail-validator.directive';

describe('MailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
